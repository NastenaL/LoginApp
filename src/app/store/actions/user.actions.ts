const CREATE_USER = '[Name] was created';
const SIGN_IN_USER = 'Sign in [Name]';

const createUser = { type: CREATE_USER };
const signIneUser = { type: SIGN_IN_USER };

export const UserActions = { createUser, signIneUser };
