export enum Path {
  // TODO: Use UpperCamelCase
  dashboard = '#/dashboard',
  home = '#',
  profile = '#/userId/profile',
  resetPassword = '#/reset-password',
  signIn = '#/sign-in',
  signUp = '#/sign-up',
}
